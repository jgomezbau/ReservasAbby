# Instala fastapi para el usuario actual
python -m pip install --user fastapi uvicorn

# O con sudo para instalarlo a nivel del sistema (requiere permiso de administrador)
# sudo python -m pip install fastapi uvicorn
